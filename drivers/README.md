# JDBC drivers
Place MariaDB and PostgreSQL JDBC `.jar` files here if your Apache Hop image/environment does not already provide the required drivers. Do not commit third-party binaries to a public repository unless licensing permits it.
